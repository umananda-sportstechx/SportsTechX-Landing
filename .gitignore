node_modules/
.next/
out/
next-env.d.ts

.env
.env.local
.env*.local

# The Figma export (~27MB). Kept out of the repo so clones stay small — get it
# from the designer / Figma and drop it here, then run `npm run design`.
STX-Figma.fig
interactions.fig

# Generated by scripts/fig-extract.mjs from STX-Figma.fig.
# Regenerate with `npm run design` rather than committing 3MB of JSON.
design/nodes.json

.DS_Store
*.tsbuildinfo

# Unlicensed retail fonts dropped in for local trials. Kept out of git so a
# deploy never serves them from our own domain.
/public/fonnts.com-*/
